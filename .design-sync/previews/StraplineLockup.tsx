import { StraplineLockup } from 'unbarrier-education';

/** The mark over unbarrier.me over "did it reach the child?". Footer only — the one place the full lockup belongs. */
export const Footer = () => (
  <div style={{ width: 360, color: 'var(--text)' }}>
    <StraplineLockup />
  </div>
);
