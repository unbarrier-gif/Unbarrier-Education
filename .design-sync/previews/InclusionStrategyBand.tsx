import { InclusionStrategyBand } from 'unbarrier-education';

/** The temporary home-page band above the chooser. Renders until 31 December 2026, then returns null. */
export const Band = () => <InclusionStrategyBand />;
