import { SayHiForm } from 'unbarrier-education';

/** The say-hi block: email, a message, and a send button. Idle state; the action resolves inertly in this build. */
export const Idle = () => <SayHiForm />;
