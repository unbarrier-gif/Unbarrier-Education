import { CredentialStrip } from 'unbarrier-education';

/** The strip under every service and information page's hero: the byline with the APLS badge, then the four credentials. */
export const Default = () => <CredentialStrip />;
