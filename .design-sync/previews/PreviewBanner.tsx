import { PreviewBanner } from 'unbarrier-education';

/** The status strip shown above the nav when a draft is opened with the preview secret. Takes no props. */
export const Banner = () => <PreviewBanner />;
