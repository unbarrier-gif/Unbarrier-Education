import { ContrastToggle } from 'unbarrier-education';

/** The nav variant: the half-filled circle and "high contrast", sized for the nav row. */
export const NavVariant = () => <ContrastToggle variant="nav" />;

/** The footer variant, as the full footer's bottom row carries it. */
export const FooterVariant = () => <ContrastToggle variant="footer" />;
